{
  "name": "kipp",
  "private": true,
  "description": "Falling blocks, but the board tips over. One HTML file.",
  "scripts": {
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "playwright": "1.50.0"
  }
}
